# REFLECTION

## 1. 对 Superpowers 的总体感受

这次项目结束后，我对 Superpowers 最大的感受是：它提供了一套完整、严密的软件工程工作流，但在当前大模型能力快速提升的背景下，这套流程对我的实际开发来说已经显得过于“重”了。

Superpowers 将 brainstorming、writing-plans、worktree、subagent-driven development、TDD、code review、verification 等环节拆得非常细。从软件工程方法论上看，这些步骤都有合理性，尤其适合约束能力较弱、容易跳步骤的模型。但在我的实际使用中，这套流程经常意味着额外的上下文、额外的 agent 调用、额外的验证和重复阅读。最终的体验往往是消耗两倍甚至更多的 token 和时间，将一个已经达到大约 95 分的结果继续优化到 96 分。对课程来说，这最后的一分可能具有方法论上的价值，但如果从真实开发的成本收益比来看，我认为很多时候并不划算。

这个判断也可能和我使用的模型有关。本项目中我主要使用 ChatGPT-5.6-Terra、ChatGPT-5.6-Sol 和 ChatGPT-5.6-Luna。随着模型自身的规划、代码理解和工具使用能力增强，我感觉很多原本需要由 Skill 显式规定的行为，已经部分被模型“内化”了。继续非常机械地执行整套 Skill 流程，有时反而增加了模型的负担。

到项目最后的修 Bug 阶段，我实际上已经放弃了严格使用 Superpowers。这不是因为我认为工程纪律没有价值，而是因为这个阶段的问题通常已经非常具体：某个状态不对、某个分支行为异常、某个接口的语义和设计不一致。此时继续走完整的 Skill 流程，收益很低，反而容易污染已经很长的上下文。

## 2. 模型选择与思考额度

我最开始主要使用 Sol，并将 reasoning 设置得很高。但很快发现一个问题：模型能力很强并不意味着应该让它一直进行最大强度的思考。

本项目在 brainstorming 阶段已经固定了大部分架构和设计基线，后面的很多任务其实只是实现明确需求。Sol 在较高思考额度下有明显的过度设计倾向，会尝试重新解释已经确定的问题，增加抽象层，或者为了理论上的完整性扩展原本并不需要扩展的边界。有时这种“思考更多”反而会让实现偏离开发基线。

后来我更多使用 Terra 负责规划和审查，并通常只开启 medium reasoning。我的体验是，对于已经有清晰 SPEC 和 PLAN 的任务，思考额度并不需要一直拉满。模型真正需要解决的是局部工程问题，而不是每次重新设计整个系统。

执行阶段我后来更多使用 Luna，并给它较高的 reasoning。选择 Luna 一个很现实的原因是价格更低，同时能力稍弱的模型有时反而更少进行不必要的发散。它更倾向于按照已经给定的任务向前执行。当然这也有代价：Luna 的稳定性和一次性解决问题的能力明显不如 Sol，一个 Sol 一轮可能完成的问题，Luna 有时需要几轮才能做到，因此时间成本未必更低。

这次经历让我意识到，模型选择不能简单理解为“越强越好”。规划、审查、实现、Debug 对模型能力的要求不同，推理预算本身也是工程资源。模型过度思考和思考不足都会产生真实成本。

## 3. Brainstorming 是 Superpowers 中最有价值的部分

如果必须选一个我认为最有价值的 Superpowers Skill，我会选择 brainstorming。

项目早期最容易出现的问题并不是代码写错，而是问题本身没有被定义清楚。Brainstorming 会不断要求确认用户、场景、边界条件、机制和验收方式，这确实帮助我在真正实现以前固定了大量设计决策。这也是后续能够让不同模型接手工作的基础。

不过我认为它与我使用过的另一个 Skill——`grill-me`——存在一定功能重叠。两者都会通过连续追问迫使开发者把模糊想法具体化。如果只让我保留一个，我目前可能会选择更轻量的 grill-me。

Superpowers 给我的一个明显负面体验是侵入性太强。安装以后，很多普通对话都会触发 Skill 判断和额外流程，甚至一个很简单的交互也可能进入一套方法论。这让我产生一种“工作区被流程污染”的感觉。工具原本应该在需要的时候提高效率，如果任何行为都被流程接管，工具本身就可能成为负担。

但有一点我不能否认：Superpowers 确实补上了我之前比较缺乏的产品开发进度管理能力。过去我更习惯直接进入实现，对任务依赖、阶段验证、完成标准以及开发记录的管理没有这么严格。这次作业迫使我把需求、计划、实现、测试、Review、Release 当成一个完整工程来看。从这个角度讲，我认为自己在项目管理能力上确实得到了提升。

## 4. TDD 与测试：必要，但不应该无限扩张

这次项目也让我重新思考了 TDD。

测试当然是必要的。尤其对于 Coding Agent Harness 这种包含状态机、反馈闭环、危险动作判断和模型调用的系统，如果没有确定性测试，很容易出现“Demo 看起来能跑，但机制其实并不可靠”的情况。Mock LLM 对这个项目尤其有价值，因为它把模型的不确定性移除以后，可以真正验证 harness 自身的代码。

问题在于，大模型很容易把“测试很重要”执行成“测试越多越好”。

特别是我提高 reasoning 额度以后，模型会不断寻找新的边界情况，为测试补测试，甚至出现一种近似“测试的测试”的倾向。每增加一层，它都有理论上的理由，但整个项目的边际收益会快速下降。过度追求形式上的测试完备性明显拖慢了我的开发速度。

所以我现在的看法是：TDD 应该服务于风险，而不是服务于覆盖率本身。核心状态转换、安全边界、失败恢复和容易回归的行为应该严格测试；非常薄的 glue code 或低风险路径则没有必要为了形式完整无限增加测试。

## 5. Subagent 与代码审查的实际效果

Subagent-driven development 是这次让我比较失望的一部分。

按照理论，多 agent 可以分别负责实现、spec compliance review 和 code quality review，从而降低单个模型的盲区。但我在项目中实际使用的两个 review subagent，几乎都没有发现后来人工测试中真正暴露出来的 Bug，反而显著拉长了开发周期。

这也暴露了目前 LLM Review 的一个问题：如果几个 agent 使用相同或相近的模型，它们经常拥有非常相似的盲区。让一个模型写代码，再让“另一个实例的同一个模型”审查，并不一定真的获得了第二种视角。

如果以后继续使用这种方式，我会优先选择不同模型厂商或明显不同能力分布的模型做交叉 Review，而不会机械地增加 subagent 数量。我认为一个真正不同的 Reviewer，可能比两个同质 subagent 更有效。

## 6. Debug 过程中暴露出的 LLM 局限

这次开发让我对当前大模型能力的波动有了非常明显的感受。

它们拥有很高的上限，但实际最容易卡住的经常并不是什么复杂算法，而是一些很小的工程问题。模型可能围绕一个错误方向验证几十分钟，尝试越来越复杂的方法，却没有重新判断最基本的假设。

有几次我只需要告诉模型一句“问题可能不在这里，去检查另一个状态的生命周期”，它很快就能解决之前长时间没有解决的问题。

因此我认为 LLM 最大的问题之一仍然是全局观较弱。它能够在给定方向上搜索得非常深，却不擅长主动判断“这个方向本身是不是错的”。而且一旦建立了某个解释，它有时会表现得非常固执，不断寻找证据维护原有判断。

这种情况下继续增加 reasoning token 通常没有意义，只是在错误方向上搜索得更彻底。人为介入真正有价值的地方，往往正是在这里：重新定义问题、切换观察尺度、指出模型忽略的变量。

另一个让我非常不满意的问题是防御性编程和防御性写作。模型经常为了覆盖假设中的各种异常情况加入大量 fallback、guard、兼容分支和解释文字。单独看每一处似乎都有理由，但累积起来会严重破坏代码结构和文本可读性。对我来说，目前没有特别理想的自动化方法解决这个问题，只能在 Review 中主动删除这些“理论上安全、实际上制造复杂度”的代码。

## 7. 对 Vibe Coding 与严格工程流程的重新理解

这次项目也让我想起过去进行 vibe coding 的经历。

Vibe coding 最大的优势是快。想到一个行为，马上让模型实现，马上看到结果，这种“所想即所得”的反馈循环非常高效。而 Superpowers 代表的是另一个极端：先定义、再计划、再测试、再实现、再 Review、再验证。

严格流程确实减少了 Bug，也显著提高了项目的可追踪性，但它同样让开发变得更累、更慢、更昂贵。

经过这次项目，我觉得真正适合 AI 辅助开发的方法应该位于两者之间。高风险、核心机制和难以回滚的部分应该严格；低风险、容易验证、快速迭代的部分应该允许更接近 vibe coding 的工作方式。

未来如果重新做这个项目，我不会完整机械地执行 Superpowers 的每一步。我会保留 brainstorming、明确的 SPEC、关键任务计划、核心机制 TDD 和最终验证，但会大幅减少重复 Review、低收益 subagent 和形式化流程。流程应该根据风险动态调整，而不是所有任务使用同样重量的方法。

## 8. 最后的认识

这次项目让我最大的改变，并不是学会了某一个 Skill，而是开始更认真地考虑如何管理 AI 开发本身。

以前我的注意力更多放在“怎么让模型把代码写出来”，现在我更关注模型选择、推理预算、上下文污染、任务粒度、Review 独立性、测试收益以及什么时候应该人工介入。

Superpowers 的很多具体流程我以后未必会继续完整使用，我甚至认为随着模型能力继续提升，其中一部分 Skill 很可能会逐渐失去存在的必要。但它让我体验了一次非常完整的软件工程流程，也让我更清楚地看到：AI 能显著降低代码实现的成本，却没有消除工程判断的价值。

模型可以快速生成方案、代码和测试，但什么时候应该停止思考、什么时候测试已经足够、什么时候一个抽象属于过度设计、什么时候整个 Debug 方向就是错的，这些判断目前仍然需要人来做。

对我来说，这可能才是这次项目最有价值的部分。

> 说明：本文内容基于本人实际项目开发经历与个人反思撰写，使用 AI 辅助进行了结构整理和语言润色。